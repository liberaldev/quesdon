apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quesdon
  namespace: argocd
spec:
  project: byulmaru
  source:
    repoURL: https://github.com/byulmaru/quesdon.git
    targetRevision: HEAD
    path: charts
  destination:
    server: https://kubernetes.default.svc
    namespace: quesdon
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
